// Initiates the menu bar
void init_menu(GtkWidget * bar, GtkAccelGroup * accel, struct Document * document, GKeyFile * config);

// Initiates search features
void search_init(struct Document * document);