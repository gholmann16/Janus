name: Build release packages

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to stamp on deb/rpm when not building from a tag (0.0.0)'
        required: false
        type: string
        default: '0.0.0'

jobs:
  appimage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y meson ninja-build gettext libgtk-3-dev libgtksourceview-4-dev squashfs-tools zsync desktop-file-utils appstream
          # Released appimage-builder doesn't yet support zstd compression, per README - install from git main.
          pip install --user git+https://github.com/AppImageCrafters/appimage-builder.git

      - name: Build
        run: |
          meson setup build --buildtype release --prefix /usr
          ninja -C build
          DESTDIR="$PWD/../AppDir" meson install -C build

      - name: Build AppImage
        run: |
          # data/AppImageBuilder.yml has a developer-machine-specific AppDir.path;
          # override it here rather than editing the tracked recipe.
          sed "s#^\( *path: \).*#\1$PWD/../AppDir#" data/AppImageBuilder.yml > /tmp/AppImageBuilder.yml
          export PATH="$HOME/.local/bin:$PATH"
          appimage-builder --recipe /tmp/AppImageBuilder.yml --appdir "$PWD/../AppDir" --build-dir /tmp/appimage-build

      - uses: actions/upload-artifact@v4
        with:
          name: janus-appimage
          path: '*.AppImage*'

  flatpak:
    runs-on: ubuntu-latest
    # The action needs flatpak-builder plus a privileged, effectively-root environment to install runtimes
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-25.08
      options: --privileged
    steps:
      - uses: actions/checkout@v4

      - name: Point manifest at the tag that triggered this build
        run: |
          sed -i "s/tag: v[0-9.]*/tag: ${GITHUB_REF_NAME}/" data/dev.pantheum.janus.yml

      - uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          bundle: janus.flatpak
          manifest-path: data/dev.pantheum.janus.yml
          cache: true

  deb:
    runs-on: ubuntu-latest
    container: debian:11
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y git ca-certificates build-essential meson ninja-build gettext libgtk-3-dev libgtksourceview-4-dev dpkg-dev appstream

      - name: Build
        run: |
          meson setup build --buildtype release --prefix /usr
          ninja -C build

      - name: Package deb
        run: |
          # branch dispatch runs (no tag) have no version to derive, so fall back
          # to the manually-supplied input instead of stamping the branch name.
          VERSION="${{ github.ref_type == 'tag' && github.ref_name || inputs.version }}"
          VERSION="${VERSION#v}"
          sed -i "s/^Version: .*/Version: $VERSION-1/" data/control
          PKGDIR="$PWD/../janus-notepad_${VERSION}-1"
          rm -rf "$PKGDIR"
          DESTDIR="$PKGDIR" meson install -C build
          install -Dm644 data/control "$PKGDIR/DEBIAN/control"
          dpkg-deb --root-owner-group -b "$PKGDIR" "janus-notepad_${VERSION}-1.deb"

      - uses: actions/upload-artifact@v4
        with:
          name: janus-deb-debian11
          path: janus-notepad_*.deb

  rpm:
    runs-on: ubuntu-latest
    container: rockylinux:8
    steps:
      # actions/checkout falls back to a git-less REST API download when git
      # isn't in PATH yet, so it has to be installed before checkout runs.
      - name: Install git
        run: dnf install -y git

      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          dnf install -y ca-certificates epel-release dnf-plugins-core
          dnf config-manager --set-enabled powertools || dnf config-manager --set-enabled crb
          dnf install -y rpm-build
          dnf builddep -y data/janus.spec

      - name: Package rpm
        run: |
          git config --global --add safe.directory '*'
          VERSION="${{ github.ref_type == 'tag' && github.ref_name || inputs.version }}"
          VERSION="${VERSION#v}"
          sed -i "s/^Version: .*/Version: $VERSION/" data/janus.spec
          mkdir -p "$HOME/rpmbuild"/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          echo '%_topdir %(echo $HOME)/rpmbuild' > "$HOME/.rpmmacros"
          cp data/janus.spec "$HOME/rpmbuild/SPECS/"
          git archive --output="$HOME/rpmbuild/SOURCES/janus-notepad-${VERSION}.tar.gz" --prefix="janus-notepad-${VERSION}/" HEAD
          rpmbuild -bb "$HOME/rpmbuild/SPECS/janus.spec"

      - uses: actions/upload-artifact@v4
        with:
          name: janus-rpm-el8
          # Container jobs get $HOME forced to /github/home by the runner, not /root.
          path: /github/home/rpmbuild/RPMS/**/*.rpm

  publish:
    needs: [appimage, flatpak, deb, rpm]
    runs-on: ubuntu-latest
    # Only touch the Releases page for tag-triggered runs; branch dispatches
    # are build-only tests and should just leave plain Actions artifacts.
    if: github.ref_type == 'tag'
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Draft a release with these assets
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          fail_on_unmatched_files: true
          draft: true
